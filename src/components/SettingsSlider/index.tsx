import { ReactElement } from "react";
import "./style.css";

export interface ISettingsSliderProps {
    currentValue: number;
    minValue: number;
    maxValue: number;
}

// export comst

const SettingsSlider = (): ReactElement => {

    return(

    )
}

export default SettingsSlider;